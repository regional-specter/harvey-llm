# Harvey chat TUI — install separately from dataset builder
#   pip install -r requirements-chat.txt

unsloth
torch
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.0; platform_system != "Darwin"
textual>=0.80.0
rich>=13.7.0
huggingface_hub>=0.34.0
